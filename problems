on the seperate password generator, the .get(0 isn't functioning properly

.get() recieves nothing, and is therefore not able to change what happens to the password in the generatePassword() function

once fixed, will make major progeress
